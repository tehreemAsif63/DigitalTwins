// Defines the structure of the data used for rendering the graph in the RowComponent.
//separate file created to ensure reusability
export interface DataType {
    time_vector: number[];
    measurement_data: number[];
}
